import { Client } from 'discord.js';
import { sendMessageToDiscord } from '../helpers/send-message-to-discord';

const onPlayerBallKick = (room: any, client: Client) => {
  room.onPlayerBallKick = (player: any) => {

    

};
};
export default onPlayerBallKick;
